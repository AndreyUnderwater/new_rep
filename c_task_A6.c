#include <stdio.h>

 
int main() {
    signed int a1, a2, out;
    scanf("%d%d", &a1, &a2);
    out = a1 - a2;
    printf("%d", out);
    //out = a1 * a2 * a3;
    //printf("%d*%d*%d=%d", a1,a2,a3,out);    
    return 0;
}
